name: Validate skill bundle

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check-refs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check references, frontmatter, and size budgets
        run: bash scripts/check-refs.sh
